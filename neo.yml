name: neo
backend: C
license: "BSD-3-Clause"
kind: Binary
version: 0.1.0
binaries: [neo]
toolchain:
  version: 2.2.0
dependencies:
  - curly >= 1.1.1
  - noise >= 0.1.10
  - yaml >= 2.2.0
  - semver >= 1.2.3
  - jsony >= 1.1.5
  - shakar >= 0.1.3
  - results >= 0.5.1
  - floof >= 1.0.0
  - pretty == 0.1.0
  - leveldb >= 0.4.1
  - url >= 0.1.0
formatter: nph
